# Fulexo Platform - Development Environment Configuration
# Copy this file to .env for local development
# ⚠️ DO NOT USE THESE VALUES IN PRODUCTION ⚠️

# =============================================================================
# 🚀 QUICK START - These defaults work out of the box for local development
# =============================================================================

# Environment mode
NODE_ENV=development

# Domain configuration (use localhost for development)
DOMAIN_API=http://localhost:3000
DOMAIN_APP=http://localhost:3001

# Database (works with docker-compose.dev.yml)
POSTGRES_DB=fulexo
POSTGRES_USER=fulexo_user
POSTGRES_PASSWORD=localdev123

# Redis/Valkey cache
REDIS_URL=redis://valkey:6379/0

# =============================================================================
# 🔐 SECURITY (Development defaults - CHANGE IN PRODUCTION!)
# =============================================================================

# JWT Secret (64+ characters) - Generated for dev, MUST change for production
JWT_SECRET=dev_jwt_secret_key_minimum_64_characters_for_local_development_only_change_this

# Encryption key (EXACTLY 32 characters) - Dev default, MUST change for production
ENCRYPTION_KEY=devkey1234567890123456789012dev!

# =============================================================================
# 🌐 API CONFIGURATION
# =============================================================================

# API URLs
NEXT_PUBLIC_API_BASE=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3001
BACKEND_API_BASE=http://api:3000
SHARE_BASE_URL=http://localhost:3001

# Additional URLs
FRONTEND_URL=http://localhost:3001
WEB_URL=http://localhost:3001

# =============================================================================
# 📦 STORAGE (MinIO - S3 compatible)
# =============================================================================

S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=fulexo-cache

# =============================================================================
# 🚢 KARRIO SHIPPING INTEGRATION
# =============================================================================

# Karrio database (separate from main DB)
KARRIO_POSTGRES_USER=karrio
KARRIO_POSTGRES_PASSWORD=karriodev123
KARRIO_POSTGRES_DB=karrio

# Karrio configuration
KARRIO_SECRET_KEY=dev_karrio_secret_key_change_in_production_minimum_32_chars
KARRIO_API_URL=http://karrio-server:5002
KARRIO_ALLOWED_HOSTS=localhost,127.0.0.1,karrio-server
KARRIO_CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:5001

# API tokens for service communication
FULEXO_TO_KARRIO_API_TOKEN=dev_token_fulexo_to_karrio_change_in_production
FULEXO_INTERNAL_API_TOKEN=dev_token_internal_api_change_in_production

# =============================================================================
# 🔧 OPTIONAL CONFIGURATION
# =============================================================================

# Monitoring (Grafana)
GF_SECURITY_ADMIN_PASSWORD=admin

# Logging
LOG_LEVEL=LOG

# Share tokens
SHARE_TOKEN_SECRET=dev_share_token_32_chars_change!
MASTER_KEY_HEX=devmasterkey0123456789abcdef0123456789abcdef0123456789abcdef0123

# Admin credentials (for seeding)
ADMIN_PASSWORD=demo123
CUSTOMER_PASSWORD=demo123
ADMIN_EMAIL=admin@example.com

# =============================================================================
# 📧 EMAIL CONFIGURATION (Optional - configure in Settings)
# =============================================================================

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# =============================================================================
# 🚚 CARRIER CREDENTIALS (Optional - configure as needed)
# =============================================================================

# UPS Integration
# UPS_USERNAME=your_ups_username
# UPS_PASSWORD=your_ups_password
# UPS_ACCESS_LICENSE_NUMBER=your_ups_license

# =============================================================================
# 📝 NOTES FOR DEVELOPERS
# =============================================================================
#
# 1. This file contains development-friendly defaults that work immediately
# 2. All secrets are weak and MUST be changed for production
# 3. Use 'docker-compose -f docker-compose.dev.yml up -d' for local development
# 4. Access the services at:
#    - Web Panel: http://localhost:3001
#    - API: http://localhost:3000
#    - MinIO Console: http://localhost:9001 (minioadmin/minioadmin)
#    - Karrio Dashboard: http://localhost:5001
#
# 5. To generate secure values for production:
#    - JWT_SECRET: openssl rand -hex 32
#    - ENCRYPTION_KEY: openssl rand -hex 16
#    - Other tokens: openssl rand -hex 32
#
# Happy coding! 🚀